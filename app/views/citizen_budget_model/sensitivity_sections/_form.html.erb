<%= form_for([@sensitivity, @sensitivity_section], html: {role: 'form'}, builder: CitizenBudgetModel::CitizenBudgetFormBuilder) do |f| %>
  <%= f.errors %>
  <%= f.translated_text_field :name %>
  <% f.buttons(context: [@sensitivity, @sensitivity_section]) %>
<% end %>

